server:
  port: 80
#  ssl:
#    key-store: classpath:keystore.p12
#    key-store-password: imperio
spring:
  application:
    name: imperio-instances
  main:
    allow-bean-definition-overriding: true
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: create-drop
  r2dbc:
    url: r2dbc:postgres://10.43.230.146:5432/imperio
    name: imperio
    username: postgres
    password: y3aFEnnH7e
    pool:
      initial-size: 10
      enabled: false
      validation-depth: local
  task:
    execution:
      pool:
        allow-core-thread-timeout: true
        core-size: 8
  devtools:
    restart:
      enabled: false

management:
  server:
    port: 80
    ssl:
      enabled: false
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      probes:
        enabled: true
      enabled: true
